package com.balatro.grammar;

/**
 * Whose run an effect targets — this run or the Nemesis's. A closed set replacing the bespoke
 * {@code toOpponent} booleans (Pizza's grant, Asteroid's delevel); the Match supplies the opponent run.
 */
public enum Side { SELF, OPPONENT }
